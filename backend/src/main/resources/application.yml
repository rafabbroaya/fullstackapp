security:
  ignored:
  - "*.bundle.*"
  user:
    name: rafael
    password: rafael
server:
  port: 8080
spring:
  session:
    store-type: redis
  redis:
    #dev
    #host: localhost
    #prod
    host: redis

